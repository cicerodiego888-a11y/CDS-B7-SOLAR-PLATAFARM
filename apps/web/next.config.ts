import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // Next.js 15.5: desativa o indicador/painel visual de Dev Tools sobre a UI.
  devIndicators: false,
};

export default nextConfig;